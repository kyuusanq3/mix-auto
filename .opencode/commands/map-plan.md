---
description: Plan-only map triage from a short symptom — no edits, no ask-loops
agent: plan
---
Short user report:

$ARGUMENTS

Required: call skill local-opencode-loop then skill local-map-triage. Output the triage template only. One class; defer extras. Do not ask which files to open.
